{{#modal-dialog title="Close stream" doNotAcceptText="No" acceptText="Yes" cancelText="Cancel" accept=(action "saveAndClose") doNotAccept=(action "close") dismiss=(action "dismiss")}}
  Do you want to save the stream <b>{{model.name}}</b> before closing it?
{{/modal-dialog}}
