<ol class="breadcrumb">
  <li>{{model.file.name}}</li>
</ol>
        
<h3>Stream</h3>

<p>
  Add operators to the graphical stream editor on the right.
</p>

<p>
  Initialize the operators and connect them. Observe operator inputs by clicking
  on the <i>eye</i> symbol and adding them to the view.
</p>

<p>
  Start the stream and select the <i>View</i> tab to see the data flow at the
  observed operator inputs.
</p>
