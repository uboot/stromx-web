{{#modal-dialog title="Remove connection" acceptText="Remove" cancelText="No" accept=(action "remove") dismiss=(action "dismiss")}}
  Do you really want to remove the connection <b>{{model.output.title}} -> {{model.input.title}}</b>?
{{/modal-dialog}}
