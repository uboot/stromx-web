{{#modal-dialog title="Remove connection" acceptText="Remove" cancelText="No" accept="remove" dismiss="dismiss"}}
  Do you really want to remove the connection <b>{{output.title}} -> {{input.title}}</b>?
{{/modal-dialog}}
